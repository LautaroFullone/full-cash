{
   "hooks": {
      "PostToolUse": [
         {
            "matcher": "Write|Edit",
            "hooks": [
               {
                  "type": "command",
                  "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && npx --prefix /Users/lautarofullone/projects/full-cash prettier --write \"$f\" --ignore-unknown; } 2>/dev/null || true",
                  "statusMessage": "Formateando con Prettier..."
               }
            ]
         }
      ],
      "Notification": [
         {
            "matcher": "",
            "hooks": [
               {
                  "type": "command",
                  "command": "osascript -e 'display notification \"Claude necesita que le respondamos\" with title \"Claude Code\"'"
               }
            ]
         }
      ]
   }
}
